/** Local SVG assets for landing page — downloaded from Figma, saved in /public. */

const B = import.meta.env.BASE_URL;

export const imgCamera01 = `${B}camera01.svg`;
export const imgUpload03 = `${B}upload03.svg`;
export const imgClose = `${B}close.svg`;
export const imgLine1 = `${B}line1.svg`;
